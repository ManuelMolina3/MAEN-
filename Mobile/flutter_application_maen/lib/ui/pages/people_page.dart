import 'package:flutter/material.dart';

class PerfilePage extends StatelessWidget {
  const PerfilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('perfile page');
  }
}
